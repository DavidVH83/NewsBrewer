"""AI provider abstraction modules for the NewsBrewer pipeline.

Wraps different LLM provider APIs (GitHub Models, OpenAI, etc.) behind a
common interface so that the rest of the pipeline is provider-agnostic.
"""
